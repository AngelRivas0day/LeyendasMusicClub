import { GroupPerCategoryPipe } from './group-per-category.pipe';

describe('GroupPerCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupPerCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
